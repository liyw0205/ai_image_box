# 目标 08：本地存储架构迁移校验

状态：待开始

## 范围

- 为 channels、agents、tasks 和历史记录的 schema_version 演进建立显式迁移入口。
- 读取旧字段时完成兼容归一化，写入时输出当前 schema。
- 对无法迁移的数据保留诊断信息，不让单条损坏记录阻断整个列表。

## 验收

- 使用旧版本 JSON 样本可以加载为当前数据模型。
- 缺失可选字段采用安全默认值。
- 错误记录只影响自身，并能在诊断包中定位。
