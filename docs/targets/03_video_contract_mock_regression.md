# 目标 03：视频接口 Mock 回归

状态：已完成（0.8.9）

## 范围

- 使用本地 MockWebServer 覆盖 Grok 通用 JSON 与 Seedance content 提交请求。
- 覆盖 submit、job 解析、poll、视频下载和失败响应。
- 固化路径模板、鉴权头和时长/参考图参数的回归契约。

## 验收

- 无需真实 API Key 即可跑通成功与失败视频链路。
- 测试断言请求路径、负载、轮询 URL 和下载字节。
- 真实账户回归只用于补充上游差异，不替代本地回归。
