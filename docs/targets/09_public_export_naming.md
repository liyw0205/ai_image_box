# 目标 09：公共导出文件名与冲突策略

状态：待开始

## 范围

- 为图片、视频、配置和诊断导出生成稳定、可读、无敏感信息的默认文件名。
- 处理同名覆盖、MediaStore 重名和 SAF 用户改名后的反馈。
- 导出结果记录只保存必要的本地元数据，不记录公共目录绝对路径。

## 验收

- 连续导出同一结果不会静默覆盖已存在文件。
- 图片、视频、JSON 与 ZIP 使用正确扩展名和 MIME 类型。
- 导出失败可区分用户取消、权限错误和 I/O 错误。
