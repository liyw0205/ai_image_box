# 目标 02：历史筛选批量复用

状态：已完成（0.8.8）

## 范围

- 为当前筛选结果提供显式多选与批量复用操作。
- 每条复用记录创建独立的新任务 ID，保留原记录参数和可用参考素材。
- 提交前汇总目标数量和不可复用原因，避免隐式批量提交。

## 验收

- 可从筛选结果选择多条记录并批量进入队列。
- 已删除渠道、缺失素材或无效模型会被逐条提示且不阻塞其他有效任务。
- 任务、历史和 attempts 能区分新的任务 ID。
