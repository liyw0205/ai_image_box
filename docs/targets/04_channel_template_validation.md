# 目标 04：渠道模板本地校验

状态：待开始

## 范围

- 保存前校验视频模板所需 Base URL、模型类型、提交路径和轮询路径模板。
- 明确提示无效 JSON、未知 video_provider 和不支持的占位符。
- 保持自定义兼容接口可扩展，不强制固定厂商字段。

## 验收

- Grok、Seedance 模板可无错误保存。
- 缺失或不合法字段会阻止保存并说明字段名。
- 校验逻辑有纯 JVM 测试。
