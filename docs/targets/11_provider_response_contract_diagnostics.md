# 目标 11：Provider 响应契约诊断

状态：待开始

## 范围

- 为各类 provider 响应建立统一的字段缺失、状态冲突和媒体候选为空诊断。
- 在任务 attempt 中记录经过脱敏的契约失败摘要，区分服务端失败与响应格式不兼容。
- 补充纯 JVM 测试覆盖图片和视频的典型异常响应。

## 验收

- 响应状态成功但没有可用素材时，历史记录显示明确原因。
- 诊断不暴露 API Key、Cookie、token 或完整远端 URL 查询参数。
- 图片与视频 adapter 的异常响应均有可复现的单元测试。
